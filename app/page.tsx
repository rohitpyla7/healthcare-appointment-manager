"use client";
import { useState } from "react";
export default function Home(){const[mode,setMode]=useState<"login"|"register">("login");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[name,setName]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMsg("");const res=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(mode==="login"?{email,password}:{name,email,password})});const data=await res.json();if(!res.ok){setMsg(data.error||"Something went wrong");return}window.location.href="/dashboard";}return <main className="shell"><div className="topbar"><div className="brand">Care<span>Flow</span></div><span className="badge">Healthcare appointment platform</span></div><section className="hero"><span className="badge">Patient · Doctor · Admin</span><h1>Appointments, visits and follow-up in one place.</h1><p>Book appointments, share symptoms before a visit, review visit notes afterwards, and keep schedules and notifications up to date.</p></section><div className="grid"><div className="card"><h2>{mode==="login"?"Sign in":"Create patient account"}</h2><form className="stack" onSubmit={submit}>{mode==="register"&&<><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} required /></>}<label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required />{msg&&<div className="notice">{msg}</div>}<button className="btn">{mode==="login"?"Sign in":"Register"}</button></form><p className="small muted">{mode==="login"?"New patient?":"Already registered?"} <button className="btn secondary" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"Create account":"Sign in"}</button></p></div><div className="card"><h3>Core capabilities</h3><ul className="muted"><li>Role-based patient, doctor and admin portals</li><li>Serializable booking transaction + slot holds</li><li>Doctor leave conflict handling</li><li>LLM pre/post visit summaries with fallback</li><li>Email retry queue + medication reminders</li><li>Google Calendar OAuth 2.0 integration</li></ul></div></div><div className="footer">Demo credentials are documented in README.md after seeding.</div></main>}
